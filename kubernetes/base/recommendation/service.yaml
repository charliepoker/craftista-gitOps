apiVersion: v1
kind: Service
metadata:
  name: recco
  labels:
    app: recommendation
    component: api
spec:
  type: ClusterIP
  selector:
    app: recommendation
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
