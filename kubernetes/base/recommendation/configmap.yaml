apiVersion: v1
kind: ConfigMap
metadata:
  name: recommendation-config
  labels:
    app: recommendation
data:
  # Use the in-namespace Service DNS name (works for dev/staging/prod)
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  REDIS_DB: "0"
  LOG_LEVEL: "INFO"
  GIN_MODE: "release"
