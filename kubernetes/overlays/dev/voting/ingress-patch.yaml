apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: voting
spec:
  tls:
    - hosts:
        - voting.dev.webdemoapp.com
      secretName: voting-dev-tls
  rules:
    - host: voting.dev.webdemoapp.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: voting
                port:
                  number: 8080
