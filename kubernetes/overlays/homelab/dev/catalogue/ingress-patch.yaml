apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: catalogue
spec:
  tls:
    - hosts:
        - catalogue.dev.webdemoapp.com
      secretName: catalogue-dev-tls
  rules:
    - host: catalogue.dev.webdemoapp.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: catalogue
                port:
                  number: 5000
