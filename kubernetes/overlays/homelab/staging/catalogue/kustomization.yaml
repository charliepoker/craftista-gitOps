apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: craftista-staging

resources:
  - ../../../../base/catalogue

commonLabels:
  environment: staging

patches:
  - path: rollout-patch.yaml
    target:
      kind: Rollout
      name: catalogue
  - path: patches.yaml
    target:
      kind: Rollout
      name: catalogue
  - path: service-patch.yaml
    target:
      kind: Service
      name: catalogue
  - path: service-patch.yaml
    target:
      kind: Service
      name: catalogue-canary
  - path: configmap-patch.yaml
    target:
      kind: ConfigMap
      name: catalogue-config
  - path: ingress-patch.yaml
    target:
      kind: Ingress
      name: catalogue
