apiVersion: apps/v1
kind: Deployment
metadata:
  name: voting
spec:
  template:
    spec:
      containers:
        - name: voting
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
