apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation
spec:
  template:
    metadata:
      annotations:
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/craftista/staging/recommendation/config"
    spec:
      containers:
        - name: recommendation
          image: 8060633493/craftista-recommendation:staging-latest
