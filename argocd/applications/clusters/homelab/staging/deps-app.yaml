apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: craftista-deps-homelab-staging
  namespace: argocd
  labels:
    app: deps
    environment: staging
    component: database
spec:
  project: craftista-homelab
  source:
    repoURL: https://github.com/charliepoker/craftista-gitops.git
    targetRevision: main
    path: kubernetes/overlays/homelab/staging/deps
  destination:
    server: https://kubernetes.default.svc
    namespace: craftista-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
