apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: craftista-catalogue-prod
  namespace: argocd
  labels:
    app: catalogue
    environment: production
    component: api
spec:
  project: craftista-homelab
  source:
    repoURL: https://github.com/charliepoker/craftista-gitops.git
    targetRevision: main
    path: kubernetes/overlays/homelab/prod/catalogue
  destination:
    server: https://kubernetes.default.svc
    namespace: craftista-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  ignoreDifferences:
    - group: argoproj.io
      kind: Rollout
      jsonPointers:
        - /spec/replicas
