# ClusterSecretStore for cluster-wide Vault integration
# This ClusterSecretStore can be referenced from any namespace
# Useful for shared secrets and cross-namespace secret management

apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-backend-cluster
spec:
  provider:
    vault:
      server: "https://10.0.0.212:8200"
      path: "secret"
      version: "v2"

      # TLS configuration for external Vault
      caBundle: ""  # Will be populated from caProvider
      caProvider:
        type: Secret
        name: vault-ca-cert
        namespace: external-secrets-operator
        key: ca.crt

      # Kubernetes authentication method
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "external-secrets-cluster"

          # Service account used by External Secrets Operator
          # This service account must exist in the external-secrets-operator namespace
          serviceAccountRef:
            name: external-secrets-sa
            namespace: external-secrets-operator

      namespace: null

---
# Additional ClusterSecretStore for GitHub Actions secrets
# Used by CI/CD pipelines to access shared secrets
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-github-actions
spec:
  provider:
    vault:
      server: "https://10.0.0.212:8200"
      path: "secret"
      version: "v2"

      caProvider:
        type: Secret
        name: vault-ca-cert
        namespace: external-secrets-operator
        key: ca.crt

      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "github-actions"

          serviceAccountRef:
            name: github-actions-sa
            namespace: external-secrets-operator

      namespace: null

---
# ClusterSecretStore for ArgoCD secrets
# Used by ArgoCD to access repository credentials and webhook secrets
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-argocd
spec:
  provider:
    vault:
      server: "https://10.0.0.212:8200"
      path: "secret"
      version: "v2"

      caProvider:
        type: Secret
        name: vault-ca-cert
        namespace: external-secrets-operator
        key: ca.crt

      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "argocd"

          serviceAccountRef:
            name: argocd-sa
            namespace: argocd

      namespace: null
