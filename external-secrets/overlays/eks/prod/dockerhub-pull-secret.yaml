apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: dockerhub-pull-secret
  labels:
    app: craftista
    component: registry
    environment: prod
spec:
  refreshInterval: 30m

  secretStoreRef:
    name: vault-backend
    kind: SecretStore

  target:
    name: dockerhub-pull-secret
    creationPolicy: Owner

    template:
      type: kubernetes.io/dockerconfigjson
      metadata:
        labels:
          app: craftista
          component: registry
          environment: prod

      data:
        .dockerconfigjson: |
          {
            "auths": {
              "https://index.docker.io/v1/": {
                "username": "{{ .dockerhub_username }}",
                "password": "{{ .dockerhub_token }}",
                "auth": "{{ printf "%s:%s" .dockerhub_username .dockerhub_token | b64enc }}"
              }
            }
          }

  data:
    - secretKey: dockerhub_username
      remoteRef:
        key: secret/data/craftista/prod/common/registry
        property: dockerhub_username

    - secretKey: dockerhub_token
      remoteRef:
        key: secret/data/craftista/prod/common/registry
        property: dockerhub_token
